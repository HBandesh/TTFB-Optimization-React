import React from 'react';

export const  Header = ({}) => {
    return(
        <header className="header">
            <h1>
                Welcome to Avengers New Recurit Portal
            </h1>
        </header>
    );
}