/**
 * @module Footer This module implements the functionality and the HTML of footer.
 */
import React from 'react';

export const Footer = ({}) => {
    return(
        <footer className="tada animated">
            <h4>I hope I was able to explain the concept :)</h4>
        </footer>
    );
}