/**
 *THIS MODULE IS NO LONGER BEING USED BUT I HAVE STILL RETAINED IT FOR FUTURE.
 */

/**
 * This class holds all the utility functions for the project.
 */
export default class Utils {
    static sleep(milliseconds = 166666) {
        var arr = [];
        for (var i = 0; i < milliseconds; i++) {
            arr.push("<li>'dummy html created'</li>");
        }
        return arr;
      }
}